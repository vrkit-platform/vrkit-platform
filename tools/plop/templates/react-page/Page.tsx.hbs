// REACT
import React from "react"

// CLSX
import clsx from "clsx"

// 3FV
import { getLogger } from "@3fv/logger-proxy"

// APP
import { AppContent } from "vrkit-app-renderer/components/page"
import AppPageContainer, { type AppPageContainerProps } from "vrkit-app-renderer/components/app-page-container"

const log = getLogger(__filename),
  { info, debug, warn, error } = log

/**
 * {{name}}Page Component Properties
 */
export interface {{name}}PageProps extends AppPageContainerProps {

}

export function {{name}}Page({className, ...other}: {{name}}PageProps) {
  return (
    <AppContent>
      <AppPageContainer className={clsx(className)} {...other}>
        {{name}}Page content here
      </AppPageContainer>
    </AppContent>
  )
}

export default {{name}}Page
