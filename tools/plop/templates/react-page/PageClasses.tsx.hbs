import { createClassNames } from "@vrkit-platform/shared-ui"

const classPrefix = "{{lowerFirst name}}Page"

export const {{lowerFirst name}}PageClasses = createClassNames(
    classPrefix,
    "root",
    "header",
    "headerDetails",
    "headerButtons",
    "headerButton",
    "content"
)

export default {{lowerFirst name}}PageClasses
