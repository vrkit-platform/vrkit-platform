syntax = "proto3";

import "Pipeline.proto";

package IRacingTools.Models.Telemetry;

message TelemetryDataFile {
  
  string id = 1;
  string alias = 2;

  int64 created_at = 5;
  int64 updated_at = 6;

  string filename = 10;
  
  // Pipeline (i.e. TRACK_MAP)
  repeated Pipeline pipelines = 50;

};


