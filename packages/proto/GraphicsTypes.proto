syntax = "proto3";

package IRacingTools.Models;


enum ImageFormat {
  IMAGE_FORMAT_UNKNOWN = 0;
  IMAGE_FORMAT_SVG = 1;
  IMAGE_FORMAT_PNG = 2;
}

enum ColorFormat {
  COLOR_FORMAT_UNKNOWN = 0;
  COLOR_FORMAT_HEX = 1;
  COLOR_FORMAT_RGB = 2;
  COLOR_FORMAT_RGBA = 3;
}
